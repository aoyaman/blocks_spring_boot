
# #https://github.com/tadmacy/h2-filebased-db-example

# # datasource
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.url=jdbc:h2:file:./db/app_db;
# spring.datasource.username=dev
# spring.datasource.password=dev
# # spring.liquibase.change-log=classpath:db/db.changelog-master.yaml

# # Use "create" or "create-drop" when you wish to recreate database on restart; use
# # "update" or "validate" when data is to be kept.
# spring.jpa.generate-ddl=false
# spring.jpa.hibernate.ddl-auto=none

# # h2 for debug tool
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console
# spring.h2.console.settings.web-allow-others=true


spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:3306/blocks
spring.datasource.username=root
spring.datasource.password=pass
spring.datasource.connectionProperties=useUnicode=true;characterEncoding=utf-8;
